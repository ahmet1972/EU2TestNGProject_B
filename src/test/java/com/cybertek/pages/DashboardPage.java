package com.cybertek.pages;

public class DashboardPage extends BasePage {
    //As the parent class has a non-paremeterized constructor child class does not need to add a constructor because
    //the parent classes constructor is to be initialised by compiler.
    //We have created this class to be able to call the required methods from the parent without overriding it.
    //
}
