package com.cybertek.pages;

public class DashboardPage extends BasePage {
    //As the parent class (BasePage) has a non-paremeterized constructor, child class (DashboardPage)does not need
    // to add any constructors as the parent's constructor is to be initialised by compiler.
    //We have created this class to be able to call the required methods from the parent without overriding it.
    //
}
